import 'package:flutter/material.dart';

class UIColors {
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static Color whiteO30 = white.withOpacity(0.3);

  static const Color backgroundColor = Color(0xFF3A4361);

  // used for unselected botBarItems
  static const Color unselectedColor = Color(0xFF979797);

  static Color success = Colors.green;
  static Color failure = Colors.red;
}
